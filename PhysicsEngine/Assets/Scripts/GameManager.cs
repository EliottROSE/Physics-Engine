using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    
     [SerializeField] private List<Reward> RewardsCollider;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
